/** @type {import('next').NextConfig} */
const nextConfig = {
  /* You can leave this empty for now, or add specific jewelry shop settings later */
  output: 'standalone',
  
  // Optional: If you have images from external domains (like MongoDB/Cloudinary)
  // images: {
  //   domains: ['res.cloudinary.com'], 
  // },
};

module.exports = nextConfig;